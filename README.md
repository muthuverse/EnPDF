# EnPDF
EnPDF is a custom C# PDF parsing engine that extracts structured data from engineering drawings using low-level parsing of PDF internals (xref, objects, streams). It applies coordinate-based analysis to identify dimensions, tolerances, notes, and tables in native PDFs without relying on external libraries.
